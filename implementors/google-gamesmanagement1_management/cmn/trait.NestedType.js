(function() {var implementors = {};
implementors['google-gamesmanagement1_management'] = [];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
